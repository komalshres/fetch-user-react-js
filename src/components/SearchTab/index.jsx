import React from "react";

const SearchTab = ({ children }) => {
  return (
    <div className="w-100 d-flex justify-content-center align-items-center p-3">
      {children}
    </div>
  );
};

export default SearchTab;
